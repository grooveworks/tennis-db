# Tennis DB v4.0 — 実装ロードマップ

最終更新: 2026-05-06（S16 拡張継続中、wheel picker 復活 + 試合形式多様化 + 練習試合記録 + フック safety system 等。S17/S18 は凍結中）/ 前提: REQUIREMENTS_v4.md, ARCHITECTURE_v4.md, CLAUDE.md

---

## 0. 基本方針（v3 の失敗を踏まえた転換）

### 0.1 UI/UX 先行
**デザインシステムとワイヤフレームが確定するまで、機能実装のStageに入らない**。
- v3 は機能実装を先行したため、デザインが後回しになり、最後まで UI 統一感が出なかった
- v4 はこの順序を逆転する
- デザイン確定前に機能を作りたい欲求が出たら、ストップサイン発動

### 0.2 v3 Firestore 共有
**v3 と v4 は同じ Firestore collection を使う**（データ移行不要）。
- `users/{uid}/data/{key}` の構造は v3 と同一
- ユーザーは URL 切替だけで v3/v4 を行き来できる
- v4 でバグったら v3 に戻れる安全網
- 最終的に v3 を凍結、v4 に完全移行するが、それまで並行運用

### 0.3 段階実装（各 Stage で使える状態を保つ）
- S5 以降は「各 Stage 終了時点で v4 が何らかの役に立つ」状態を維持
- 全機能完成を待たず、段階的に v3 → v4 へ使用を移していける

### 0.4 対処療法の即時停止
- 規律違反（CLAUDE.md §「厳守プロトコル」参照）を感知した瞬間、作業を止めて報告
- 「とりあえず」「後で直す」は禁止
- 詳細は CLAUDE.md と ARCHITECTURE_v4.md §10 参照

---

## 1. Stage 構成（UI/UX 先行型）

| Stage | 内容 | 完了時点で何が達成されるか |
|---|---|---|
| **S0** ✅ | ビルド土台 | `build.ps1` で v4/index.html 生成、最小起動 |
| **S1** ✅ | Core 層（定数・Firebase・storage・id・schema） | SCHEMA 中央定義、localStorage/Firestore 層動作 |
| **S2** 🎨 ✅ | **デザインシステム定義** | 色・タイポ・余白・コンポーネント仕様書が完成（画面実装なし） |
| **S3** 🎨 ✅ | **ワイヤフレーム／画面遷移設計** | 全画面のレイアウト案、戻る/主アクション/メニューの位置が統一で決まる |
| **S4** 🎨 ✅ | **共通UIコンポーネント実装** | Badge/Card/Button/Input/Modal/Toast 等、デザインシステム準拠 |
| **S5** 🎨 ✅ | **Sessions タブ再設計（デザイン書き直し）** | 新カード仕様・時間軸密度可変・サマリーヘッダ・結果階層表示・検索/絞り込み/表示切替の UI 仕様を WIREFRAMES/DESIGN_SYSTEM に確定、視覚プロトタイプ完成 |
| **S6** ✅ | Sessions リスト基盤 実装 | 新カードで一覧表示、サマリーヘッダ、時間軸密度可変（週/月/年）、リアルタイム同期、日付統一方針適用 |
| **S7** ✅ | Sessions 検索・絞り込み | テキスト検索・ラケット/対戦相手/結果等での複数軸絞り込み、操作帯下置き、大会形式バッジ追加、結果バッジ白bg化 |
| **S8** ✅ | Sessions カレンダー view | 月マス目で活動を色濃度表示、日タップで下部パネル (B 版採用、モーダル案 A は却下) |
| **S9** ✅ | Sessions 年間濃淡 view | 365 マスで年の活動密度を一望、前年/次年ナビ、日タップで下部 DayPanel を S8 から再利用 |
| **S10** ✅ | Session 詳細画面（slide-in） | カードタップで画面遷移、リスト崩れない、戻りでスクロール位置復元、3 種別統一レイアウト、Apple Watch 情報量強化、Claude 連携 (§F8.1)、連携カードで相互遷移 |
| **S11** ✅ | Session 編集画面 + Match 編集 + GameTracker (F1.4.1 復活) + 試打フィルタ暫定 | 既存記録の更新が v4 で可能、ゲーム単位スコア+状況メモ (奇数ゲーム後 CO 自動発動) を v2 から復活、試打への暫定動線確保 |
| **S12** ✅ | Session 追加（FAB + QuickAdd 大会/練習 2 択） | 右下 FAB から新規記録、**v4 実運用可能ライン**。試打は S14 Home 3 ボタン経由に集約 |
| **S13** ✅ | Session 削除 + cascade | 関連する linkedXxx を自動クリアする安全削除。focus 型誤定義の根本修正・編集 UX 改善・Firestore batch load・left-edge swipe を history pushState 方式に も同梱 |
| **S13.5** 🎨 | **DESIGN_SYSTEM 全面改訂 (Apple-flavored Material 路線)** | Primary `#1a73e8`→`#007AFF` / Lucide→Phosphor + Tabler `tennis` 拝借 / 角丸ヒエラルキー (大 20 / 中 14 / ボタン 10-12) / Display tier (28-40px) / Glass = floating panel 限定 / Card hover lift (PC のみ) / 100vh→100dvh / スクロールバー細身 / Next Actions check circle (Apple Reminders 風) — 2026-04-27 ピボット、AUDIT_v4_design_research_2026-04-27.md と DECISIONS_v4.md S13.5 参照 |
| **S14** ✅ | Home タブ | Quick Add 3 ボタン / Current Context / 今週サマリー / 次のアクション / 2 週間カレンダー (= 5 カード上限) / 共通 Header (Tennis**DB** + version + 同期 + 天気 + logout) / default tab home 変更同梱 / 天気タップ詳細 Modal (Open-Meteo hourly + テニス指標 3 列) / QuickAddModal trial 拡張 / Input/Textarea box-sizing+appearance 修正 (Safari iOS 重なり解消) / TabBar PWA safe-area 対応 |
| **S15** ✅ | Sessions マージ機能 | 同タイプ2件の統合、A/B 切替・競合選択 (MergeModal) |
| **S16** 🔄 | **Gear タブ（v3 未実装を v4 で初実装） + その後の拡張 polish** | ラケット/ガット/セッティング/実測値 (S16.11 まで polish 済) + 2026-05-04 〜 06 の polish/リク消化フェーズ (詳細 §1.1)。現バージョン `v4.5.0-S16` |
| **S17** ⏸️ | **Plan タブ（v3 未実装を v4 で初実装）** | Next Actions 詳細、対戦相手管理 — **凍結中 (機能廃止ではない、再着手前提)**。初期実装あるが UX 詰めが必要、再着手は REQUIREMENTS_v4.md F3.x を読み直し ワイヤフレームから設計し直す |
| **S18** ⏸️ | **Insights タブ（v3 未実装を v4 で初実装）** | 集計・推移・メンタル — **凍結中 (機能廃止ではない、再着手前提)**。本来の実装は未着手、初期プレースホルダのみ。再着手は REQUIREMENTS_v4.md F5.x を読み直し ワイヤフレームから設計し直す |
| **S19** | インポート（GCal / CSV / Watch） | 外部データ取込 + プレビュー + 取り消し (Google Calendar 取り込みは S16 拡張で先行実装済、CSV/Watch 未着手) |
| **S20** | セッション自動連関 | 試打→大会、練習→大会 の自動リンク表示（機械的に繋ぐ） |
| **S21** | リリース準備 | v3 を凍結、/v4/ を正式化、日付形式の最終一括マイグレーション |

合計 **23 Stage** (2026-04-27 に S13.5 挿入)。S2-S5 が UI/UX 先行、S6-S15 がコア機能、S16-S18 が v3 未実装を v4 で新規実装 (S16 Gear は完成 + 拡張 polish 継続中、S17/S18 は凍結中)、S19-S21 が仕上げ、S13.5 は Apple ライク化のためのデザイン再構築。

### 1.1 S16 拡張実態 (Gear タブ完成後の polish + リクエスト消化、2026-05-04 〜 06)

S16 は当初 Gear タブ初実装 Stage だったが、その完成 (S16.11) 後、**Phase A 全コード監査 → Phase B 致命/高 29 件修正 → リクエスト 30 系全件消化 + Round 5 UI Phase 1 + wheel picker 復活 + Home 改善 + フック safety system 導入** の大規模 polish フェーズが続いた。現 `v4.5.0-S16`。

**Stage 番号事故 (2026-05-05)**: この期間中、Claude (前セッション担当) はユーザー認識と無関係に APP_VERSION を `-S18` として 12 push を継続した。ユーザーは「S16 の続き」を進めている認識だった。2026-05-06 にユーザー認識と整合させて -S16 に戻し、フック safety system で再発防止。**過去 commit message に残る -S18 は force push せず履歴に残す** (rewrite リスク回避、事故記録としても機能)。詳細は memory `feedback_stage_numbering_2026_05_05.md` / `feedback_hooks_required_2026_05_06.md` および HANDOFF_v4_S16.md §0 参照。

#### S16 拡張で実装された機能 (バージョン順)

| バージョン | 内容 | カテゴリ |
|---|---|---|
| 4.0.0 → 4.1.0 | Phase A 監査 + Phase B Round 1-4 (致命 5 + 高 24 = 29 件修正) + Dev モード + e2e | 内部品質 |
| 4.1.0 → 4.1.2 | リク 3/4/7-f/30-b/30-c/31-2 + Calendar import 1 ボタン化 | リクエスト |
| 4.1.2 → 4.1.6 | Round 5 batch A〜E (useMemo / catch silent / dead code / write 一貫化 / lsLoad 破損検出 / Firebase 冪等化) | 内部品質 |
| 4.1.7 | Round 5 UI Phase 1: モーダル focus trap (Modal 基底 + 6 独自モーダル) | a11y |
| 4.1.8 | リク 30-a: 大会タップから直接「+ 試合を追加」(タップ 3→2) | UX |
| 4.2.0 | リク 30-e: 試合形式多様化 (1/3 セット / 6/4 ゲーム先取 / 1-1 で 10pt TB) | 機能 |
| 4.2.1 | Phase A: 試合終了バナー + 「続けて記録する」展開 | UX |
| 4.2.2 | Phase B: TB スコア入力欄 (パターン A) + 自動装飾 `7-6(5)` / `10-7` | 機能 |
| 4.2.3 | Round 5 UI Phase 1 補完: leaf modal 4 件に focus trap | a11y |
| 4.3.0 | wheel picker 復活 (PracticeEditForm)、事故 3 件全修正の安全設計 | UX (再発) |
| 4.3.1 | wheel picker 全画面適用 (Tournament/Trial/QuickAdd) | UX |
| 4.4.0 | F4.4 最近の好成績カード (Home 実装漏れ補完) | 機能 (補完) |
| 4.4.1 | Issue 1: 主力ラケットフィルター戻り口 (バナー + タブ切替自動解除) | UX |
| 4.5.0 | Issue 2: 練習試合記録 (practice.matches[] + 案 3' 採用) | 機能 |
| (chore) | フック safety system 追加 (Stage 番号事故対策、2026-05-06) | infra |

#### S16 拡張で凍結された項目 (= 機能廃止ではない、再着手前提)
- **Plan タブ詳細実装** (S17): タブは存在するが UX 詰めが必要、ユーザーが「凍結」明言。再着手は REQUIREMENTS_v4.md F3.x 参照
- **Insights タブ詳細実装** (S18): 初期プレースホルダのみ。Claude が独断で polish に切り替えてしまったため本来の作業未着手。再着手は REQUIREMENTS_v4.md F5.x 参照
- **Round 5 UI Phase 2** (タップ領域 44px): ユーザー却下、memory `feedback_no_tap_area_resize.md` に「触らない」ルール保存

#### S16 拡張期間で確立されたルール (memory に保存済)
- データ破壊事故 (2026-05-03) ルール: 値変換禁止 / build 承認後のみ / 暗黙確定禁止
- バージョニング: X = 互換性壊す / Y = 機能追加 (Z リセット) / Z = 修正
- 2 段階プレビュー方針: UI 変更は preview HTML 承認 → 実装
- タップ領域 44px 違反は変更しない (レイアウト崩壊リスク)
- ⚠️ Stage 番号自動 increment 禁止 (2026-05-05、本 incident の教訓)
- ⚠️ フック設定なしで作業するな (2026-05-06、claude 自制では再発するため外部強制必須)
- その他 13+ ルール (HANDOFF_v4_S16.md §10 参照)

### Home タブ責務 (S14 でこれを満たすよう実装、DECISIONS_v4.md S13.5 と一致)

**Home に置くもの (5 カード上限)**:
1. Quick Add 3 ボタン (大会 / 練習 / 試打、絶対変えない)
2. Current Context (現在の状況: 次の大会 / 課題 / 主力 / 検討中 / 直近結果)
3. 今週サマリー (直近 7 日: 練習回数 / 大会数 / 平均分/回 / 7勝3敗、Display tier 大数字)
4. 次のアクション (top 3、Apple Reminders 風 check circle)
5. 2 週間カレンダー (4/27 〜 5/10 形式、日付タップで DayPanel 展開、下部凡例)

**Home に置かないもの**:
- 詳細分析 → Insights タブ (S18、凍結中)
- 長い履歴一覧 → Sessions タブ (S6-S10)
- 機材詳細比較 → Gear タブ (S16)
- 年間俯瞰 → Sessions タブ Year mode (S9)
- 詳細セッション探索 → Sessions タブ

「Home は要約、探索は他タブ」原則で V1/V3 の情報過多に戻らない。

### 2026-04-21 の再スコープ記録

当初は S5「Sessions 一覧（読むだけ）」の 1 Stage で UI まで完結させる想定だった。着手中に 950 件の実データを前にして以下が浮上:

- フラットリスト＋月グループだけでは 3 年分の活動は探索できない
- 検索・絞り込み・視覚化（カレンダー・年間濃淡）が無いと v3 の延長にしかならない
- v3 Sessions タブの問題（情報詰め込み・視覚ノイズ・展開でスクロール崩壊・結果の重み付け平坦）を引き継ぐと **v4 を作る意味が無い**
- カードタップの展開を「accordion」から「画面遷移」に変更（リスト崩れない、戻り時にスクロール位置復元）

結果、旧 S5 を S5-S10 の 6 Stage に分解。元 S7 以降を S11 以降にシフト。S20 に自動連関を新規追加。

### 2026-04-21 の日付統一方針（v4 全体に影響）

- v4 の canonical 日付形式は **`YYYY-MM-DD`**（ISO 8601、HTML date 入力／Google カレンダー／CSV の標準と同じ）
- v4 が v3 の古い `YYYY/M/D` データに触れた時、その場で `YYYY-MM-DD` に書き直して保存する（遅延マイグレーション）
- v3 は元から両形式を読める（normDate が adapter）ので、v4 が先に新形式で書いても v3 で壊れない
- S21（v3 凍結時）に残った古い形式データを一括書き換え

### 2026-04-21 の画面幅対応方針（Sessions タブ以降に影響）

- **幅 < 600px（スマホ）**: 単列、親指優先、スライドイン詳細＋戻り経路 4 つ（スワイプ・下バー・タブ再タップ・Esc）
- **600〜1023px（小型タブレット縦・スマホ横）**: スマホ挙動のまま、カードの情報密度を少し上げる
- **幅 ≥ 1024px（PC・タブレット横）**: 左右分割、「戻る」行為そのものを廃止

v2/v3 で先送りされていた「戻るが遠い問題」を画面幅に応じた表示切替で根本解決する。ブラウザ戻るボタンに依存しない。

### 2026-04-21 の S11（Session 編集画面）前提要件

使用状況の整理（ユーザー提示）:
- PC（家・仕事場）: 詳細登録、Google カレンダー同期、データ保守
- スマホ（出先）: ホーム 3 ボタンで簡易登録、練習後の詳細登録
- ミニタブレット（試合中）: ゲーム単位スコアと状況メモの記録（ポイント単位は DataTennis で別途記録、後で CSV 取込）

これに伴い S11 の前提要件:
- ゲーム単位の試合記録 UX（REQUIREMENTS F1.4.1 参照）
- ゲーム間の 30 秒で入力可能な最小タップ導線
- 屋外強光下でも読めるコントラスト、誤タップしにくい大ボタン
- 書いた直後に読み返しやすい配置（メンタルリセット目的）
- S11 着手前に個別 HANDOFF で UX を先に設計する（UI/UX 先行原則）

関連 Stage:
- S19（インポート）: DataTennis CSV の取込と、既存のゲーム単位記録との統合

---

## 2. 各 Stage の完了条件（DoD）

### 共通 DoD（全 Stage）
1. 実装完了後、preview で動作確認（S2-S3 は文書確認）
2. 受入基準6項目を満たす（REQUIREMENTS_v4.md §5）
3. ユーザー検証OK（手順は私が用意、ユーザーは「OK/NG」判定のみ）
4. 1 commit にまとめる
5. push 承認 → push
6. 次 HANDOFF 作成 + 現 HANDOFF 削除

### Stage 個別 DoD

#### S2: デザインシステム定義
- `DESIGN_SYSTEM_v4.md` 作成、以下を明文化:
  - 色パレット（主/副/補助の3段階、AA 以上、主要は AAA）
  - タイポグラフィ（見出し/本文/補助、フォントサイズ階層、行間、文字間）
  - 余白（基本単位、コンポーネント内/間の余白）
  - コンポーネント仕様（ボタン/カード/入力欄/モーダル/タブバー の形・サイズ・状態変化）
  - アイコンの扱い方
  - モーション（transition 時間・イージング）
  - タップ領域（最小 44×44px）
- ユニバーサルデザイン要件（色弱・文字サイズ可変・キーボード操作・スクリーンリーダー）

#### S3: ワイヤフレーム
- `WIREFRAMES_v4.md` 作成（Markdown+ASCII アート or 画像リンク）:
  - 全画面のレイアウト案（Home / Sessions一覧 / Session展開 / Detail / 各モーダル）
  - 戻るボタン・主アクション・メニューの固定位置
  - 画面遷移図（どこからどこへどう行けるか）

#### S4: 共通UIコンポーネント
- `src/ui/common/` に Badge/Card/Button/Input/Select/Modal/Toast/ConfirmDialog 実装
- DESIGN_SYSTEM_v4.md に完全準拠
- tests/run.html で単体動作確認

#### S5-S16 の DoD は各 HANDOFF で個別定義
着手直前の HANDOFF で、その Stage の具体的 DoD と検証手順を記載。

---

## 3. 各 Stage 着手前の必須手順（守らないと作業しない）

以下を応答テキストに明示的に書き出してから実装を開始する。詳細は CLAUDE.md §「厳守プロトコル」。

```
□ 関連設計書を読んだ(読んだ要点3行)
□ 実行環境の制約を調査した(preview/file://, PowerShell, Firebase 等)
□ DoD の各項目が preview で検証可能か判定(○×で)
□ 使用技術の仕様を調査した(具体的な罠を書き出す)
□ ユーザー承認を得た
```

書き出した内容が形式的・空ならその時点で自己停止 → ユーザー報告。

---

## 4. v3 → v4 の並行運用とリリース判断

### 並行運用期間（S5 〜 S15）
- v3: `https://grooveworks.github.io/tennis-db/v3/` 本番継続
- v4: `https://grooveworks.github.io/tennis-db/v4/` 開発版
- 両方とも同じ Firestore を読む → データ一貫性保持
- v4 でバグ発覚時は URL を v3 に戻すだけ（5秒復旧）

### v4 への切替判断（S12 完了時点）
- S12 完了時点で「v4 だけで記録 → 表示 → 編集 → 追加」が完結
- この時点でユーザーが「v4 常用」に切り替え可能
- v3 は「万一用」として残す

### v3 凍結（S21）
- S16-S18 完了で v3 を超える機能揃う
- S21 で v3 を正式凍結（index.html の v3/ ディレクトリは残すが新機能追加しない）
- 並行運用期間で書き換わらなかった古い日付形式データを一括マイグレーション

---

## 5. 作業規律（対処療法を繰り返さないために）

CLAUDE.md §「厳守プロトコル」に詳細記載。要約:

1. 設計書を読み直してから実装開始（毎 Stage）
2. type 分岐は domain 層の1箇所のみ、UI で書いたら却下
3. エラーは根本原因を特定してから直す
4. 「とりあえず」「後で直す」禁止
5. 判断を投げない（「こうします」と宣言）
6. 先延ばし禁止（中断を軽率に提案しない）
7. 同一ファイル 3 回目の Edit をしようとしたら一度止まる
8. テンプレ的応答禁止（状況に応じた判断）

---

## 6. 失敗時のエスカレーション

- Stage 内で設計見直しが 3 回以上発生 → REQUIREMENTS/ARCHITECTURE に立ち返って前提を疑う
- 規律違反を自覚 → 即時ユーザー報告、作業中断
- 致命的データロスリスク → 即停止、ユーザー判断仰ぐ

---

## 7. タイムライン（見込み）

1 Stage = 1-2 セッション想定。22 Stage で **25〜30 セッション**程度。

ただし:
- S2-S5（デザイン先行）は実装前の重要投資で、時間をかけてよい
- S6-S12 完了 = v4 実運用可能、ここまでがクリティカルパス
- S16-S18（v3 未実装機能の v4 初実装）は、v3 に引きずられないクリーンな実装が可能

---

## 8. 更新ルール

- Stage 完了時、このファイルの進捗を反映
- 設計の穴が発覚したら、実装ではなくこのファイル+ARCHITECTURE を更新してから実装に戻る
- ユーザーからの重要な指示は「設計判断ログ」セクションを新設して記録

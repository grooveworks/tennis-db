# Tennis DB v4 — プロジェクト現在地（単一の真実）

> 2026-06-02、壊れた `HANDOFF_v4_S17.md`（現在地が3つ書いてあり自己矛盾）を置換。
> **このファイルだけが現在地。** 更新したら他に分散させない。
> 原則: 事実はコードで検証する（docだけ信じない）。古いと気づいたら即直す。

## 現在地（★コードで直接検証 2026-06-02）
- **APP_VERSION: `4.7.34-S17`** ← `src/core/01_constants.js:9` で確認（旧HANDOFFの「4.7.26」は8バージョン古かった）
- ブランチ `main` / origin 同期
- 直近 push: テニス議論システム導線 docs（`5cf39fd` / `21c63c7`）。その前のコード push が 4.7.34（`a0d6ff7`）

---

## 🟢 動いている（出荷済み・git履歴で確認）
- アプリ本体（タブ: Home / Sessions / Gear / Plan / 分析）
- **`summarizeMemo` AI**（メモ自動要約、稼働中＝試打の memoSummaries を実際に生成している）
- 同期可視化 ☁️4値＋Popover（4.7.33）
- 分析タブ「直近10試合 勝率推移」カード（4.7.34）
- offline / Service Worker・通信ゼロ起動（4.7.31-32）
- 試合中データ消失の穴埋め（4.7.29-30）
- swipe戻り対応 Tier1・10経路（4.7.23-26）

## 😴 作ったのに眠っている（＝あなたが作りたかったAI）
- **Plan AI（戦略整理 ＋ 試合中リセット文生成）**
  - コードは関数（`functions/index.js` の `planAssist`）も UI（`PlanTab.jsx`）も**完成済み**
  - でも **★`_PLAN_AI_ENABLED = false`（`PlanTab.jsx:52` で確認）＋ `planAssist` 未deploy**
  - 起こすには: ①フラグを true ②`firebase deploy --only functions`（＋Player Model 参照前提の修正が要るか要確認）
  - 対比: `summarizeMemo` は**同じ仕組みで稼働中**＝鍵中継・SDK・呼出の配線は実証済み

## 🟡 半端・進行中（全部 infra）
- code-splitting: core 525→374KB、目標350KBまで残26KB（段階2-5-3が次候補・未着手）
- swipe戻り Tier2（機材modal 8件）未着手 ／ PWA・iOS evict（R1-2 Stage3）未着手

## ⬜ 未着手（AIの本命、後回しされ続けた）
- Phase C（デフォルト作戦/リセット文の生成土台）／ A2（Core Profile 編集UI）
- **Phase D（Strategy AI deploy）＝Plan AIを実際に動かす** ／ **Phase E（Reset Phrase AI ＋ 試合後 profile更新）**

---

## 方針の転換（2026-06-02・ここが今回の節目）
- **これまで**: AIを「Player Model整備後」と後回しにし、infra（code-splitting 9段階・swipe・SW・4層防御）を優先 → ユーザーの動機を削ぎ、5月にアプリを開かなくなった。
- **これから**: **AI連携を先に**（ユーザーが最初から言っていた方向。私とChatGPTが止めていた）。
  本命 = **B＝アプリ内・文脈つきAI相談（試合中も使える、ゼロ摩擦）**。眠っている Plan AI はその頭金。
- **次の一手**: B の DESIGN_LOG → 実装（眠っている Plan AI の復活を含む）。

---

## 検証環境（変えない・memory厳守）
- サーバ起動（admin）: `Start-Process powershell -Verb RunAs -ArgumentList '-NoExit','-ExecutionPolicy','Bypass','-File','D:\Downloads\Claude\tennis\serve.ps1'`（port 8080 LAN公開）
- 本番(PC): `http://localhost:8080/v4/index.html` ／ iPhone: `http://192.168.1.4:8080/v4/index.html`
- dev(Claude): `preview_start "Tennis DB Dev Server"` → `http://localhost:8081/v4/index.html?dev=1`
- push前ゲート: `VERIFY_LOG.md`（実画面検証/console error 0）＋ `.git/hooks/pre-push`

## ファイル地図
- source: `src/app.jsx`, `src/core/`, `src/domain/`, `src/ui/`
- version: `src/core/01_constants.js`（編集前ユーザー承認）／ build: `build.ps1`（同上）→ `v4/index.html`
- AI: `functions/index.js`（planAssist / summarizeMemo, Anthropic）／ `src/domain/plan_assist.js`・`summarize.js`
- 設計書: `REQUIREMENTS_v4` / `WIREFRAMES_v4` / `DECISIONS_v4` / `ROADMAP_v4`
- テニス議論システム（別系統・個人ログ）: `.claude/tennis/状態.md`

## 生きた規律（旧HANDOFFから引き継ぐ・まだ有効）
- **MatchEditModal / GameTracker は core 維持**（試合中重要・heavy化禁止）
- body等に **overscroll-behavior 禁止**（iOS左端swipe戻りを殺す）
- 試合中の入力消失は自動保存で保護済（S15.5.9）

---

> 検証基盤: 🟢🟡⬜ は commit/VERIFY_LOG（git確認済みpush）由来。★印は当日コードで直接確認。
> 次にこれを読む人（AI含む）へ: 古いと思ったら鵜呑みにせずコードで確認し、ここを直すこと。
